/** @module std */
import { ListContainer } from "../base/container/ListContainer";
import { IDequeContainer } from "../base/container/IDequeContainer";
import { _IListAlgorithm } from "../base/disposable/IListAlgorithm";
import { ListIterator } from "../base/iterator/ListIterator";
import { ReverseIterator as ReverseIteratorBase } from "../base/iterator/ReverseIterator";
import { IForwardIterator } from "../iterator/IForwardIterator";
/**
 * Doubly Linked List.
 *
 * @author Jeongho Nam <http://samchon.org>
 */
export declare class List<T> extends ListContainer<T, List<T>, List.Iterator<T>, List.ReverseIterator<T>> implements IDequeContainer<T, List<T>, List.Iterator<T>, List.ReverseIterator<T>>, _IListAlgorithm<T, List<T>> {
    /**
     * @hidden
     */
    private ptr_;
    /**
     * Default Constructor.
     */
    constructor();
    /**
     * Initializer Constructor.
     *
     * @param items Items to assign.
     */
    constructor(items: Array<T>);
    /**
     * Copy Constructor
     *
     * @param obj Object to copy.
     */
    constructor(obj: List<T>);
    /**
     * Fill Constructor.
     *
     * @param size Initial size.
     * @param val Value to fill.
     */
    constructor(size: number, val: T);
    /**
     * Range Constructor.
     *
     * @param first Input iterator of the first position.
     * @param last Input iteartor of the last position.
     */
    constructor(first: Readonly<IForwardIterator<T>>, last: Readonly<IForwardIterator<T>>);
    /**
     * @hidden
     */
    protected _Create_iterator(prev: List.Iterator<T>, next: List.Iterator<T>, val: T): List.Iterator<T>;
    /**
     * @inheritDoc
     */
    front(): T;
    /**
     * @inheritDoc
     */
    front(val: T): void;
    /**
     * @inheritDoc
     */
    back(): T;
    /**
     * @inheritDoc
     */
    back(val: T): void;
    /**
     * @inheritDoc
     */
    unique(binary_pred?: (x: T, y: T) => boolean): void;
    /**
     * @inheritDoc
     */
    remove(val: T): void;
    /**
     * @inheritDoc
     */
    remove_if(pred: (val: T) => boolean): void;
    /**
     * @inheritDoc
     */
    merge(source: List<T>, comp?: (x: T, y: T) => boolean): void;
    /**
     * Transfer elements.
     *
     * @param pos Position to insert.
     * @param from Target container to transfer.
     */
    splice(pos: List.Iterator<T>, from: List<T>): void;
    /**
     * Transfer a single element.
     *
     * @param pos Position to insert.
     * @param from Target container to transfer.
     * @param it Position of the single element to transfer.
     */
    splice(pos: List.Iterator<T>, from: List<T>, it: List.Iterator<T>): void;
    /**
     * Transfer range elements.
     *
     * @param pos Position to insert.
     * @param from Target container to transfer.
     * @param first Range of the first position to transfer.
     * @param last Rangee of the last position to transfer.
     */
    splice(pos: List.Iterator<T>, from: List<T>, first: List.Iterator<T>, last: List.Iterator<T>): void;
    /**
     * @inheritDoc
     */
    sort(comp?: (x: T, y: T) => boolean): void;
    /**
     * @hidden
     */
    private _Quick_sort;
    /**
     * @hidden
     */
    private _Quick_sort_partition;
    /**
     * @inheritDoc
     */
    reverse(): void;
    /**
     * @inheritDoc
     */
    swap(obj: List<T>): void;
}
export declare namespace List {
    /**
     * Iterator of the List.
     *
     * @author Jeongho Nam <http://samchon.org>
     */
    class Iterator<T> extends ListIterator<T, List<T>, Iterator<T>, ReverseIterator<T>, T> {
        /**
         * @hidden
         */
        private source_ptr_;
        /**
         * @hidden
         */
        private constructor();
        /**
         * @inheritDoc
         */
        reverse(): ReverseIterator<T>;
        /**
         * @inheritDoc
         */
        source(): List<T>;
        /**
         * @inheritDoc
         */
        /**
        * @inheritDoc
        */
        value: T;
        equals(obj: Iterator<T>): boolean;
    }
    /**
     * Reverse iterator of the List.
     *
     * @author Jeongho Nam <http://samchon.org>
     */
    class ReverseIterator<T> extends ReverseIteratorBase<T, List<T>, Iterator<T>, ReverseIterator<T>, T> {
        /**
         * @hidden
         */
        protected _Create_neighbor(base: Iterator<T>): ReverseIterator<T>;
        /**
         * @inheritDoc
         */
        /**
        * @inheritDoc
        */
        value: T;
    }
}
export import list = List;
//# sourceMappingURL=List.d.ts.map